// Odd and Even number detector.

START
    INPUT number
    
    IF number MOD 2 EQUALS 0 THEN
        OUTPUT "Even"
    ELSE
        OUTPUT "Odd"
    END IF
ENDSTART
    INPUT number
    
    IF number MOD 2 EQUALS 0 THEN
        OUTPUT "Even"
    ELSE
        OUTPUT "Odd"
    END IF
END